#include<stdio.h>

int fact(int n);



int main()
{
    int n;
    printf("enter n");

    scanf("%d",&n);

    printf("%d",fact(n));
}








int fact(int n)
{
    if(n==1)
    {
        return 1;
    }
   int  f_1=fact(n-1);
    int f = f_1*n;
    return f;
}
